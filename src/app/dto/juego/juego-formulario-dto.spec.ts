import { JuegoFormularioDto } from './juego-formulario-dto';

describe('JuegoFormularioDto', () => {
  it('should create an instance', () => {
    expect(new JuegoFormularioDto()).toBeTruthy();
  });
});
